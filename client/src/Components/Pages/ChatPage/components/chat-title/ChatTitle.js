import React from 'react';

import './ChatTitle.scss';

const ChatTitle = () => {
  

    return (
        <div id="chat-title">
            <span>USER NAME</span>
        </div>
    );
}

export default ChatTitle;